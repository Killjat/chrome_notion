# 🚀 AI驱动的敏捷开发工作流程

## 📋 概述

这是一套基于AI协作的软件工程管理方法，通过规范化的流程确保项目从需求到部署的全生命周期管理。该工作流程已在用户信息检测器项目中验证，适用于与各种大模型的协作开发。

## 🎯 核心原则

1. **看板驱动管理** (Kanban-Driven Management) - 🔥 **最重要**
2. **规范驱动开发** (Spec-Driven Development)
3. **实时任务管理** (Real-time Task Management)
4. **阶段性交付** (Incremental Delivery)
5. **自动化部署** (Automated Deployment)
6. **持续验证** (Continuous Validation)

### 🏆 看板管理的核心地位

**看板管理工具是整个工作流程的指挥中心**，所有开发活动都围绕看板进行：
- 📋 **任务可视化**：所有任务状态一目了然
- 🔄 **进度实时跟踪**：每个任务的进展实时更新
- 👥 **协作透明化**：团队成员和AI都能看到项目状态
- 📊 **数据驱动决策**：基于看板数据优化工作流程

---

## 📊 工作流程架构

```
需求分析 → 界面设计 → 系统设计 → 任务分解 → 开发实现 → 测试验证 → 部署上线
    ↓         ↓         ↓         ↓         ↓         ↓         ↓
  规范文档   🎨UI设计   技术设计   📋看板创建  📋看板执行  📋看板验证  📋看板完成
```

### 🎯 看板管理贯穿全流程

**每个阶段都必须在看板上体现：**
1. **需求阶段**：创建需求任务卡片
2. **设计阶段**：创建设计任务卡片  
3. **开发阶段**：创建开发任务卡片
4. **测试阶段**：创建测试任务卡片
5. **部署阶段**：创建部署任务卡片

**看板状态流转：**
```
📝 待办 (Todo) → 🔄 进行中 (In Progress) → ✅ 已完成 (Done)
```

---

## 🔄 详细工作流程

### 阶段1：需求分析与规范制定

#### 1.1 创建项目规范目录
```bash
mkdir -p .kiro/specs/[project-name]
```

#### 1.2 编写需求文档 (requirements.md)
**模板结构：**
```markdown
# 项目需求规范

## 项目概述
- 项目名称
- 项目目标
- 核心功能

## 功能需求
### 功能1：[功能名称]
- **描述**：功能详细描述
- **用户故事**：作为[用户角色]，我希望[功能描述]，以便[价值体现]
- **验收标准**：
  - [ ] 标准1
  - [ ] 标准2

## 技术需求
- 技术栈选择
- 性能要求
- 兼容性要求

## 约束条件
- 时间约束
- 资源约束
- 技术约束
```

### 阶段2：界面设计与用户体验 🎨 **新增重要阶段**

#### 2.1 创建UI设计文档 (ui-design.md)
**模板结构：**
```markdown
# UI/UX设计文档

## 设计理念
- 设计风格：现代简约/商务专业/活泼可爱等
- 色彩方案：主色调、辅助色、强调色
- 字体选择：标题字体、正文字体、代码字体
- 设计原则：易用性、美观性、一致性

## 页面设计

### 主页设计
**布局结构：**
```
┌─────────────────────────────────────────┐
│              网站标题/Logo               │
├─────────────────────────────────────────┤
│  🌐 IP检测    🖥️ 系统检测    🔍 指纹检测  │
├─────────────────────────────────────────┤
│                                         │
│           检测结果展示区域               │
│                                         │
├─────────────────────────────────────────┤
│          操作按钮区域                    │
└─────────────────────────────────────────┘
```

**视觉设计要求：**
- 卡片式布局，圆角阴影
- 渐变背景或纯色背景
- 图标 + 文字的信息展示
- 响应式设计适配移动端

### 看板页面设计
**布局结构：**
```
┌─────────────────────────────────────────┐
│           📋 项目看板管理                │
├─────────┬─────────┬─────────┬───────────┤
│ 📝 待办  │🔄进行中 │✅已完成 │ 📊 统计   │
│         │         │         │           │
│ [任务1] │ [任务3] │ [任务5] │ 总计: 8   │
│ [任务2] │ [任务4] │ [任务6] │ 完成: 3   │
│         │         │         │ 进行: 2   │
└─────────┴─────────┴─────────┴───────────┘
```

## 组件设计规范

### 信息卡片 (InfoCard)
- 尺寸：最小高度120px，宽度自适应
- 边框：1px solid #e0e0e0，圆角8px
- 阴影：0 2px 8px rgba(0,0,0,0.1)
- 内边距：16px
- 图标：24x24px，位于左上角
- 标题：16px 粗体，颜色 #333
- 内容：14px 常规，颜色 #666

### 按钮设计
- 主按钮：蓝色背景 #007bff，白色文字
- 次按钮：白色背景，蓝色边框和文字
- 危险按钮：红色背景 #dc3545，白色文字
- 尺寸：高度40px，内边距12px 24px
- 圆角：4px，悬停效果

### 色彩方案
- 主色：#007bff (蓝色)
- 成功色：#28a745 (绿色)
- 警告色：#ffc107 (黄色)
- 危险色：#dc3545 (红色)
- 文字色：#333333 (深灰)
- 背景色：#f8f9fa (浅灰)

## 交互设计

### 动画效果
- 页面加载：淡入效果 0.3s
- 卡片悬停：轻微上浮 + 阴影加深
- 按钮点击：按下效果 0.1s
- 数据加载：旋转loading图标

### 响应式断点
- 桌面端：>= 1200px
- 平板端：768px - 1199px  
- 手机端：< 768px
```

#### 2.2 创建设计原型图

**使用工具推荐：**
- **Figma**：在线协作设计工具
- **Sketch**：Mac专业设计工具
- **Adobe XD**：跨平台设计工具
- **即时设计**：国产在线设计工具

**原型图要求：**
1. **线框图 (Wireframe)**：页面布局和功能结构
2. **视觉稿 (Mockup)**：完整的视觉设计
3. **交互原型**：可点击的交互演示
4. **设计规范**：颜色、字体、间距等规范

#### 2.3 设计评审与确认

**评审清单：**
- [ ] 设计风格是否符合项目定位
- [ ] 用户体验是否流畅直观
- [ ] 视觉层次是否清晰
- [ ] 响应式设计是否完整
- [ ] 无障碍访问是否考虑
- [ ] 品牌一致性是否保持

### 阶段3：系统设计与架构规划

#### 3.1 编写技术设计文档 (design.md)
**模板结构：**
```markdown
# 系统设计文档

## 系统架构
- 整体架构图
- 技术栈选择
- 数据流设计

## 模块设计
### 前端模块
- 组件结构
- 状态管理
- 路由设计

### 后端模块
- API设计
- 数据库设计
- 服务架构

## 部署架构
- 部署环境
- 容器化方案
- 监控方案
```

### 阶段4：任务分解与看板管理 🔥 **核心阶段**

#### 4.1 看板系统搭建（必须第一步完成）

**看板系统是项目管理的核心基础设施，必须在所有开发工作开始前搭建完成。**

**核心功能要求：**
- ✅ **三列布局**：待办 (Todo) / 进行中 (In Progress) / 已完成 (Done)
- ✅ **拖拽功能**：支持任务卡片在列之间拖拽
- ✅ **实时更新**：任务状态变化立即反映
- ✅ **任务详情**：每个任务包含标题、描述、创建时间
- ✅ **数据持久化**：使用数据库保存任务状态
- ✅ **独立访问**：独立的URL路径（如 `/kanban`）
- ✅ **统计功能**：显示各状态任务数量

**技术实现标准：**
```javascript
// 后端：Node.js + SQLite + Express
// 数据库表结构
CREATE TABLE tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT,
  status TEXT DEFAULT 'todo',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

// 前端：React + React DnD
// 核心组件：KanbanBoard, TaskCard, Column
```

#### 4.2 编写任务文档 (tasks.md)
**模板结构：**
```markdown
# 项目任务分解

## 📋 看板管理说明
- **看板地址**：http://localhost:3001/kanban
- **使用规则**：
  1. 所有任务必须先在看板上创建
  2. 开始工作前将任务移至"进行中"
  3. 完成后立即移至"已完成"
  4. 每个任务必须包含验收标准

## 开发阶段划分

### 🎯 阶段0：看板系统搭建（优先级最高）
**目标**：建立项目管理基础设施
**看板任务**：
- [ ] 📋 搭建看板后端API
- [ ] 📋 实现看板前端界面
- [ ] 📋 配置数据库和路由
- [ ] 📋 测试看板功能完整性

### 🎯 阶段1：[功能阶段名称]
**目标**：阶段目标描述
**看板任务**：
- [ ] 📋 任务1：具体任务描述 + 验收标准
- [ ] 📋 任务2：具体任务描述 + 验收标准

### 🎯 阶段2：[功能阶段名称]
**目标**：阶段目标描述
**看板任务**：
- [ ] 📋 任务1：具体任务描述 + 验收标准
- [ ] 📋 任务2：具体任务描述 + 验收标准
```

#### 4.3 看板操作规范

**AI执行任务的标准流程：**
1. **任务创建**：在看板上创建新任务卡片
2. **状态更新**：开始工作时移至"进行中"
3. **进度汇报**：定期更新任务进展
4. **完成确认**：功能测试通过后移至"已完成"
5. **结果验证**：提供测试截图或演示链接

**看板任务卡片标准格式：**
```
标题：[阶段X] 功能名称
描述：
- 具体实现内容
- 验收标准
- 预计完成时间
```

### 阶段5：开发实现与进度管理

#### 5.1 看板驱动的开发流程规范 🔥 **严格执行**

**每个开发任务的标准流程：**

1. **📋 任务领取**
   ```
   - 从看板"待办"列选择任务
   - 确认任务描述和验收标准
   - 将任务拖拽至"进行中"列
   ```

2. **🔄 状态同步**
   ```
   - 开始编码前：更新任务状态为"进行中"
   - 编码过程中：如有问题及时在任务中备注
   - 遇到阻塞：在任务描述中说明阻塞原因
   ```

3. **💻 代码实现**
   ```
   - 按照设计文档实现功能
   - 遵循代码质量标准
   - 实现错误处理机制
   ```

4. **🧪 自我测试**
   ```
   - 验证功能完整性
   - 测试边界条件
   - 确保验收标准达成
   ```

5. **✅ 任务完成**
   ```
   - 将任务拖拽至"已完成"列
   - 在任务中添加完成说明
   - 提供测试截图或演示链接
   ```

#### 5.2 看板实时汇报模板

**每完成一个任务必须按此格式汇报：**
```
🎯 看板更新通知

📋 任务：[任务标题]
🔄 状态：待办 → 进行中 → ✅ 已完成

🔧 实现内容：
- 功能点1：具体实现描述
- 功能点2：具体实现描述

📊 测试结果：
- 测试场景1：✅ 通过
- 测试场景2：✅ 通过

🔗 验证方式：
- 访问地址：http://localhost:3001/xxx
- 操作步骤：1. xxx 2. xxx 3. xxx

📈 看板统计：
- 待办：X个任务
- 进行中：X个任务  
- 已完成：X个任务

🎯 下一步：开始执行下一个任务
```

#### 5.3 看板数据驱动决策

**基于看板数据优化工作：**
- **进行中任务过多**：说明任务粒度太大，需要拆分
- **完成任务较少**：说明开发效率需要提升
- **待办任务堆积**：说明需要重新评估优先级

### 阶段6：测试验证与质量保证

#### 6.1 功能测试清单
- [ ] 核心功能验证
- [ ] 边界条件测试
- [ ] 错误处理测试
- [ ] 兼容性测试
- [ ] 性能测试

#### 6.2 用户验收测试
- 按照需求文档的验收标准逐项验证
- 记录测试结果和问题
- 及时修复发现的问题

### 阶段7：部署上线与运维监控

#### 7.1 零环境差异部署 🐳 **推荐方案**

**Docker容器化部署的优势：**
- ✅ **环境一致性**：本地和服务器运行完全相同的容器
- ✅ **零配置差异**：无需关心系统类型和版本  
- ✅ **依赖隔离**：所有依赖都打包在容器内
- ✅ **一键部署**：任何系统都用相同的命令
- ✅ **版本锁定**：Node.js版本完全一致

**部署步骤：**
```bash
# 1. 上传项目到服务器
scp -r ./project root@server:/root/

# 2. 执行一键部署
chmod +x deploy-docker-simple.sh
sudo ./deploy-docker-simple.sh

# 3. 访问应用
# http://server-ip:3001
```

#### 7.2 传统部署方案 (备选)

**适用场景：**
- 无法使用Docker的环境
- 需要直接控制系统资源
- 特殊的系统要求
**必需文件：**
- `Dockerfile`：容器化配置
- `docker-compose.yml`：服务编排
- `nginx.conf`：反向代理配置
- `ecosystem.config.js`：PM2进程管理
- `.env.production`：生产环境变量
- `deploy.sh`：自动化部署脚本
- `DEPLOYMENT.md`：部署文档

#### 7.2 部署流程
1. **环境准备**：服务器配置、域名解析
2. **代码部署**：上传代码、安装依赖
3. **服务启动**：启动应用服务
4. **功能验证**：验证部署成功
5. **监控配置**：设置日志和监控

---

## 🛠️ 工具和技术栈

### 项目管理工具
- **📋 看板系统**：自建React看板 + SQLite（核心工具）
- **📝 文档管理**：Markdown规范文档
- **🔄 版本控制**：Git + GitHub
- **📊 进度跟踪**：基于看板的实时统计

### 看板系统技术规范
```javascript
// 必需的API端点
GET    /api/tasks        // 获取所有任务
POST   /api/tasks        // 创建新任务
PUT    /api/tasks/:id    // 更新任务状态
DELETE /api/tasks/:id    // 删除任务
GET    /api/tasks/stats  // 获取统计数据

// 必需的前端组件
- KanbanBoard.js         // 看板主组件
- TaskCard.js           // 任务卡片组件
- Column.js             // 列组件
- TaskForm.js           // 任务创建表单
```

### 开发技术栈
- **前端**：React + CSS3 + HTML5
- **后端**：Node.js + Express + SQLite
- **部署**：Docker + Nginx + PM2

### AI协作工具
- **代码生成**：各种大模型（GPT、Claude、Kiro等）
- **文档生成**：AI辅助文档编写
- **问题解决**：AI辅助调试和优化

---

## 📝 AI协作指令模板

### 项目启动指令
```
我要开发一个[项目类型]，主要功能是[核心功能描述]。
请按照规范驱动开发的方法，先创建项目规范文档，
然后搭建看板管理系统，再分阶段实现功能。
所有开发进度必须在看板上实时体现。
```

### UI设计指令
```
在开始技术开发前，请先创建UI设计文档和原型图。
包括：页面布局设计、色彩方案、组件规范、交互效果。
使用Figma或类似工具创建可视化设计稿。
确保设计符合现代Web标准和用户体验最佳实践。
```

### 设计评审指令
```
请展示UI设计稿，包括：
1. 主要页面的视觉设计
2. 组件库和设计规范
3. 响应式布局方案
4. 交互动画效果说明
设计确认后再开始编码实现。
```
### 看板优先指令
```
先搭建看板工具，我需要在看板工具上看到任务的进度。
看板必须包含：待办、进行中、已完成三列，
支持拖拽操作，数据持久化保存。
```

### 阶段执行指令
```
继续工作，按照现在的工作流程进行工作，
中间我不参与，自己确认执行。
每完成一个任务必须在看板上更新状态，
并提供完成证明和测试结果。
```

### 看板状态检查指令
```
请检查看板当前状态，汇报：
1. 各列任务数量统计
2. 当前进行中的任务
3. 最近完成的任务
4. 下一步计划执行的任务
```

### 部署指令
```
现在我要把整个项目部署在公网服务器上，
请创建完整的部署配置和文档。
```

---

## ✅ 质量检查清单

### 需求阶段检查
- [ ] 需求文档完整清晰
- [ ] 用户故事明确
- [ ] 验收标准可测试
- [ ] 技术选型合理

### 设计阶段检查
- [ ] 系统架构清晰
- [ ] 模块划分合理
- [ ] 接口设计完整
- [ ] 数据库设计规范

### 开发阶段检查
- [ ] 代码质量良好
- [ ] 功能实现完整
- [ ] 错误处理完善
- [ ] 测试覆盖充分

### 部署阶段检查
- [ ] 部署文档完整
- [ ] 配置文件正确
- [ ] 安全措施到位
- [ ] 监控机制完善

---

## 🎯 成功案例：用户信息检测器项目

### 项目概述
- **项目名称**：用户信息检测器
- **开发周期**：1天
- **技术栈**：React + Node.js + SQLite
- **部署方式**：Docker + Nginx

### 实施效果
- ✅ 需求明确，无返工
- ✅ 进度可控，实时跟踪
- ✅ 质量保证，功能完整
- ✅ 部署顺利，文档完善

### 关键成功因素
1. **🎨 设计先行**：UI设计必须在编码前完成
2. **📋 看板优先**：看板系统必须第一个搭建完成
3. **📝 规范先行**：先写文档再写代码
4. **🔄 任务细化**：大任务拆分为小任务
5. **📊 实时跟踪**：看板实时反映进度
6. **🐳 Docker部署**：容器化消除环境差异 ⭐ **新增**
7. **✅ 状态同步**：每个操作都要更新看板状态

### Docker部署的核心价值 🐳 **重要**
- **🎯 零环境差异**：本地macOS和服务器Linux运行一致
- **⚡ 快速部署**：5分钟完成部署，成功率95%+
- **🔧 简化运维**：统一的管理命令和监控方式
- **📦 依赖隔离**：避免版本冲突和权限问题
- **🚀 标准化流程**：任何系统都用相同的部署方式

### 界面设计的核心价值
- **👁️ 视觉美观**：提升用户第一印象和使用体验
- **🎯 用户友好**：直观的交互设计降低学习成本
- **� 响应适配**：多设备兼容确保广泛可用性
- **🎨 品牌一致**：统一的设计语言建立专业形象
- **♿ 无障碍访问**：考虑特殊用户群体的使用需求

---

## 🔄 持续改进

### 流程优化建议
1. 根据项目复杂度调整阶段划分
2. 根据团队规模调整协作方式
3. 根据技术栈调整工具选择
4. 根据反馈持续优化流程

### 扩展应用
- 适用于各种Web应用开发
- 适用于移动应用开发
- 适用于API服务开发
- 适用于数据分析项目

---

## 📞 使用说明

1. **复制此文档**到新项目目录
2. **根据项目特点**调整模板内容
3. **与AI协作时**使用标准指令
4. **严格按照流程**执行各个阶段
5. **及时记录经验**持续改进流程

---

**版本**：v1.0  
**创建日期**：2026年1月28日  
**适用范围**：AI协作软件开发项目  
**维护者**：项目团队

---

## 🎯 看板管理系统详细规范

### 📋 看板系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    看板管理系统                              │
├─────────────────┬─────────────────┬─────────────────────────┤
│   📝 待办        │  🔄 进行中       │    ✅ 已完成            │
│   (Todo)        │  (In Progress)  │    (Done)              │
├─────────────────┼─────────────────┼─────────────────────────┤
│ • 任务1          │ • 任务3          │ • 任务5                 │
│ • 任务2          │ • 任务4          │ • 任务6                 │
│ • 任务...        │                 │ • 任务...               │
└─────────────────┴─────────────────┴─────────────────────────┘
```

### 🔧 看板功能清单

#### 必需功能 (Must Have)
- ✅ **三列布局**：待办/进行中/已完成
- ✅ **拖拽操作**：任务卡片可在列间拖拽
- ✅ **任务CRUD**：创建/读取/更新/删除任务
- ✅ **数据持久化**：SQLite数据库存储
- ✅ **实时更新**：状态变化立即保存
- ✅ **统计显示**：各列任务数量统计

#### 推荐功能 (Should Have)
- 🔄 **任务历史**：记录任务状态变更历史
- 🏷️ **任务标签**：支持任务分类标签
- ⏰ **时间跟踪**：记录任务创建和完成时间
- 🔍 **搜索过滤**：支持任务搜索和过滤
- 📊 **进度图表**：可视化项目进度

#### 可选功能 (Could Have)
- 👤 **任务分配**：支持多人协作
- 📅 **截止日期**：任务截止时间管理
- 📎 **文件附件**：任务相关文件上传
- 💬 **评论功能**：任务讨论和备注
- 📧 **通知提醒**：任务状态变化通知

### 📊 看板数据模型

```sql
-- 任务表
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,                    -- 任务标题
    description TEXT,                       -- 任务描述
    status TEXT DEFAULT 'todo',             -- 状态：todo/inprogress/done
    priority INTEGER DEFAULT 1,            -- 优先级：1-5
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at DATETIME                   -- 完成时间
);

-- 任务历史表
CREATE TABLE task_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER,
    old_status TEXT,
    new_status TEXT,
    changed_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (task_id) REFERENCES tasks(id)
);
```

### 🎨 看板UI设计规范

#### 布局要求
- **响应式设计**：支持桌面和移动端
- **拖拽友好**：清晰的拖拽区域和反馈
- **状态区分**：不同状态用不同颜色区分
- **信息层次**：标题、描述、时间等信息层次清晰

#### 交互规范
- **拖拽操作**：平滑的拖拽动画效果
- **状态反馈**：操作成功/失败的明确反馈
- **加载状态**：数据加载时的loading状态
- **错误处理**：网络错误等异常情况处理

### 🔄 看板操作流程图

```
开始项目
    ↓
创建看板系统
    ↓
添加项目任务到"待办"列
    ↓
选择任务开始工作
    ↓
将任务拖拽到"进行中"列
    ↓
完成任务开发和测试
    ↓
将任务拖拽到"已完成"列
    ↓
更新任务完成信息
    ↓
选择下一个任务 → 回到"选择任务"步骤
    ↓
所有任务完成 → 项目结束
```

### 📈 看板效率指标

#### 关键指标 (KPI)
- **任务完成率**：已完成任务 / 总任务数
- **平均完成时间**：任务从创建到完成的平均时长
- **工作负载**：进行中任务数量
- **任务流转速度**：单位时间内完成的任务数

#### 优化建议
- **完成率低于80%**：检查任务拆分是否合理
- **平均完成时间过长**：任务粒度可能太大
- **进行中任务过多**：可能存在并行工作过多问题
- **流转速度慢**：需要分析瓶颈环节

---

## 🚀 看板系统快速搭建指南

### 1. 后端API实现 (15分钟)
```javascript
// server/routes/tasks.js
const express = require('express');
const router = express.Router();
const db = require('../database/db');

// 获取所有任务
router.get('/', (req, res) => {
    db.all('SELECT * FROM tasks ORDER BY created_at DESC', (err, rows) => {
        if (err) return res.status(500).json({ error: err.message });
        res.json(rows);
    });
});

// 创建任务
router.post('/', (req, res) => {
    const { title, description } = req.body;
    db.run('INSERT INTO tasks (title, description) VALUES (?, ?)', 
           [title, description], function(err) {
        if (err) return res.status(500).json({ error: err.message });
        res.json({ id: this.lastID, title, description, status: 'todo' });
    });
});

// 更新任务状态
router.put('/:id', (req, res) => {
    const { status } = req.body;
    const completedAt = status === 'done' ? new Date().toISOString() : null;
    
    db.run('UPDATE tasks SET status = ?, completed_at = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?',
           [status, completedAt, req.params.id], function(err) {
        if (err) return res.status(500).json({ error: err.message });
        res.json({ success: true });
    });
});

module.exports = router;
```

### 2. 前端组件实现 (30分钟)
```jsx
// client/src/components/KanbanBoard.js
import React, { useState, useEffect } from 'react';
import { DndProvider } from 'react-dnd';
import { HTML5Backend } from 'react-dnd-html5-backend';
import Column from './Column';
import './KanbanBoard.css';

const KanbanBoard = () => {
    const [tasks, setTasks] = useState([]);
    
    useEffect(() => {
        fetchTasks();
    }, []);
    
    const fetchTasks = async () => {
        const response = await fetch('/api/tasks');
        const data = await response.json();
        setTasks(data);
    };
    
    const moveTask = async (taskId, newStatus) => {
        await fetch(`/api/tasks/${taskId}`, {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ status: newStatus })
        });
        fetchTasks();
    };
    
    const columns = [
        { id: 'todo', title: '📝 待办', status: 'todo' },
        { id: 'inprogress', title: '🔄 进行中', status: 'inprogress' },
        { id: 'done', title: '✅ 已完成', status: 'done' }
    ];
    
    return (
        <DndProvider backend={HTML5Backend}>
            <div className="kanban-board">
                <h1>📋 项目看板</h1>
                <div className="columns">
                    {columns.map(column => (
                        <Column
                            key={column.id}
                            column={column}
                            tasks={tasks.filter(task => task.status === column.status)}
                            onMoveTask={moveTask}
                        />
                    ))}
                </div>
            </div>
        </DndProvider>
    );
};

export default KanbanBoard;
```

### 3. 部署配置 (5分钟)
```javascript
// server/server.js 添加路由
app.use('/api/tasks', require('./routes/tasks'));

// 看板页面路由
app.get('/kanban', (req, res) => {
    res.sendFile(path.join(__dirname, 'public/kanban.html'));
});
```

**总计搭建时间：约50分钟**
**验证方式：访问 `/kanban` 页面，测试拖拽功能**

---

**重要提醒：看板系统是整个工作流程的基石，必须优先搭建并确保功能完整！**